import React from 'react';
import ChatContainer from './ChatContainer';

const App = () => {
  return (
    <div className="app-container">
      <ChatContainer />
    </div>
  );
};

export default App;